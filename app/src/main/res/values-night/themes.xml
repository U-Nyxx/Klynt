<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform parent: the UI is 100% Compose (KlyntTheme). This theme
         only covers the pre-Compose window. MDC was removed (diet). -->
    <style name="Theme.Klynt" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
